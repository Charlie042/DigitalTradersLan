/** Per-topic accent color and mock progress for dashboard topic cards */
export const topicAccents: Record<string, { color: string; progress: number }> = {
  candlesticks: { color: '#1B4DFF', progress: 65 },
  'market-structure': { color: '#FF3D5A', progress: 30 },
  liquidity: { color: '#7C3AED', progress: 10 },
};
